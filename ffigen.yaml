# Run with `dart run ffigen --config ffigen.yaml`.
name: ZenohDartBindings
description: |
  Bindings for `src/zenoh_ffi.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
# output: "lib/zenoh_ffi_bindings_generated.dart"
output:
  bindings: "lib/src/gen/zenoh_ffi_bindings_generated.dart"
  symbol-file:
    output: "lib/src/gen/zenoh_ffi_bindings_generated_symbols.yaml"
    import-path: "package:zenoh_ffi/src/gen/zenoh_ffi_bindings_generated.dart"

headers:
  entry-points:
    - "src/zenoh_ffi.h"
  include-directives:
    - "src/zenoh_ffi.h"

preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full

compiler-opts:
  - "-Isrc/include"
  - "-Wno-nullability-completeness"
